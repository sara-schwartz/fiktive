# TEST DOUBLE only — psych LPR is not this step
id: t_psyk_diag
name: "Psych LPR diag (test double)"
one_row_per: expand_from_parent
columns:
  - id: recnum
    name: recnum
    type: character
    role: join_key
