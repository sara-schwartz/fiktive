# TEST DOUBLE — large nomenclature, not enumerated.
id: atc
enumerated: false
values_complete: false
values_from:
  kind: package
  candidates: [codeCollection]
  dataset: ATCKoodit
lookup: null
structure:
  pattern: "letter, 2 digits, letter, letter, 2 digits"
