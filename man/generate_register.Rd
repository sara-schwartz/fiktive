% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate-api.R
\name{generate_register}
\alias{generate_register}
\title{Generate a fictitious register table}
\usage{
generate_register(
  register,
  population,
  schema,
  from,
  to,
  seed = NULL,
  scenario = NULL,
  fidelity = c("clean", "messy"),
  na_rate = NULL,
  outlier_rate = NULL,
  realistic = FALSE
)
}
\arguments{
\item{register}{Lowercase register id (fastreg name), e.g. \code{"bef"}.}

\item{population}{Persons table from \code{\link[=generate_background_population]{generate_background_population()}}.}

\item{schema}{Schema from \code{\link[=load_registers_schema]{load_registers_schema()}}.}

\item{from}{Start of the requested window (Date or coercible).}

\item{to}{End of the requested window (Date or coercible).}

\item{seed}{Optional RNG seed. Restored on exit.}

\item{scenario}{\code{NULL} (independence) or a \code{fiktive_scenario} from the
scenario_* constructors.}

\item{fidelity}{\code{"clean"} (default) or \code{"messy"}. Under scenarios, prefer
clean; messy is for pipeline stress and must not be read as moving
estimands.}

\item{na_rate, outlier_rate}{Optional fidelity rate overrides in \verb{[0, 1]}.}

\item{realistic}{Default \code{FALSE}: structural noise only, uniform across
valid codes (e.g. \code{kom} drawn evenly across municipalities). \code{TRUE}
opts into a small set of real-world-shaped defaults: \code{kom} weighted by
real municipality population, and diagnosis codes (\code{icd10}/\code{icd10_sks})
never assigned a chapter that's impossible for the patient's sex or age
(e.g. a pregnancy code to a man). Does not change what any scenario/
truth claims — see \code{?scenario_association} if you want a planted,
documented relationship instead of realistic-looking background shape.}
}
\value{
A tibble whose columns are a subset of the schema column names
for \code{register}. Zero rows is a valid event or child table.
}
\description{
\code{scenario = NULL} is independence: structurally valid noise that joins.
Pass a \code{\link[=scenario_association]{scenario_association()}}, \code{\link[=scenario_confounding]{scenario_confounding()}},
\code{\link[=scenario_mnar]{scenario_mnar()}}, or \code{\link[=scenario_complete_case]{scenario_complete_case()}} object to overlay a
known DGP after structural generation. Coefficients live only on the
scenario — never in schema YAML.
}
\details{
Snapshot grains: \code{bef} (quarterly), \code{udda} and \code{akm} (annual).
Event-from-person: \code{dod}, \code{lmdb}, \code{vnds}, \code{cancer}, \code{mfr} / Levendefoedte
(empty tables are valid; coverage ends 2018), \code{lab_dm_forsker}
(\code{analysiscode} from LabTerm / published NPU; coverage 2008-2025).
Expand-from-parent: LPR2 (\code{lpr_adm} then \code{lpr_diag} / \code{lpr_sksopr} /
\code{lpr_sksube}) and LPR3 (\code{lpr_a_kontakt} then \code{lpr_a_diagnose} /
\code{lpr_a_procregistrering}). Diagnoses/procedures are generated off the
\strong{same} contact table that was written. Household-year: \code{faik} (one row
per \code{familie_id} × year; \code{pnr} blank when present). Branch on \code{code_system}
id: \code{icd10_sks} → \code{sksr::SKS_labels} Prefix \code{dia} (D-prefixed, e.g. DE119);
plain \code{icd10} → WHO via \code{codeCollection::ICD10Koodit} (E119, never sksr);
\code{icd8} / \code{previous_code_system} until 1993 → honour or SCHEMA GAP.
Procedures sample \code{sksr} Prefix \code{opr} / related. LMDB \code{atc} samples WHO-form
codes from \code{codeCollection::ATCKoodit} (or WHOCC dump). Dispatch prefers
schema \code{one_row_per} when present. Psych LPR (\verb{t_psyk_*}) is not this step.
Never mix \code{vnds} with \code{vnds_hist} / \code{vnds_ind} / \code{vnds_ud}. Other schema
registers error as not implemented; unknown ids / novel grains are a
SCHEMA GAP.
}
