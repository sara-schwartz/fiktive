% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/catalogue-labterm.R
\name{load_labterm_catalogue}
\alias{load_labterm_catalogue}
\title{Load LabTerm / NPU analyte codes}
\usage{
load_labterm_catalogue(
  path = NULL,
  cache_dir = NULL,
  required = TRUE,
  fetch_ifcc = NULL
)
}
\arguments{
\item{path}{Path to a LabTerm / IFCC CSV (or character vector of codes).}

\item{cache_dir}{Runtime cache under \code{\link[tools:userdir]{tools::R_user_dir()}}.}

\item{required}{If \code{TRUE} (default), missing catalogue is a SCHEMA GAP.}

\item{fetch_ifcc}{If \code{TRUE}, attempt the public IFCC NPU CSV when no local
dump is configured. Default: option \code{fiktive.labterm_fetch_ifcc}, else
\code{FALSE} (tests and offline use stay deterministic).}
}
\value{
A list with \code{codes}, \code{version}, \code{source}, \code{path}. \code{NULL} when
\code{required = FALSE} and nothing is found.
}
\description{
\code{lab_dm_forsker.analysiscode} is NPU or Danish DNK terminology. PLAN locks
LabTerm (SDS) as the external catalogue SoT — do not invent code lists.
Point \code{path}, option \code{fiktive.labterm}, or env \code{FIKTIVE_LABTERM} at a LabTerm
publication CSV/XML or the public IFCC C-NPU CSV. Optional
\code{FIKTIVE_LABTERM_URL} (or the IFCC default when \code{fetch_ifcc = TRUE}) downloads
a published dump into the user cache. There is no CRAN LabTerm package.
}
