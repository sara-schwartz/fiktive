% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write-out.R
\name{write_register}
\alias{write_register}
\title{Write a generated register table to disk}
\usage{
write_register(data, path, format = c("csv", "parquet"), hive_year = FALSE)
}
\arguments{
\item{data}{A tibble from \code{\link[=generate_register]{generate_register()}}, \code{\link[=generate_registers]{generate_registers()}},
or \code{\link[=generate_custom_register]{generate_custom_register()}}.}

\item{path}{Output file path (csv/parquet) or directory (when
\code{hive_year = TRUE}).}

\item{format}{\code{"csv"} (default) or \code{"parquet"}.}

\item{hive_year}{If \code{TRUE}, write parquet hive partitions \verb{year=YYYY/}
under \code{path} (requires a \code{year} column and the arrow package).}
}
\value{
Invisibly, the path written (file or hive root).
}
\description{
Default on-disk format is \strong{CSV}. Parquet and optional hive \verb{year=}
partitioning (via arrow) are \strong{opt-in}. Stamps (schema SHA, seed, package
version, catalogue when present) are written as a \code{.meta.yaml} sidecar next
to the data file (or under the hive root as \verb{_fiktive_meta.yaml}).
Generators still return in-memory tibbles; this only persists.
}
