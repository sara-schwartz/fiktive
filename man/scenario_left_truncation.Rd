% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/truth.R
\name{scenario_left_truncation}
\alias{scenario_left_truncation}
\title{Left truncation bias scenario (STEP 9)}
\usage{
scenario_left_truncation(
  shape = 5,
  scale = 80,
  true_hazard_ratio = 1.5,
  max_entry_age = 70,
  id = "left_truncation",
  version = 1L
)
}
\arguments{
\item{shape}{Weibull shape for the age-at-event distribution (positive;
\code{1} = constant hazard, i.e. no bias to demonstrate — pick something
bigger, e.g. the default \code{5}, for an age-varying hazard).}

\item{scale}{Weibull scale for the age-at-event distribution (positive;
a characteristic age, e.g. \code{80} for a lifetime/mortality-style
outcome).}

\item{true_hazard_ratio}{True causal hazard ratio for \code{group} (positive;
must not be \code{1} for this bias to have anything to bias — see Details).}

\item{max_entry_age}{People enter at a uniformly random age between \code{0}
and this value (positive) — the delayed-entry / left-truncation
mechanism. Bigger values make the bias more pronounced.}

\item{id}{Scenario id (default \code{"left_truncation"}).}

\item{version}{Integer scenario version.}
}
\value{
A list of class \code{fiktive_scenario}.
}
\description{
Generates a \strong{time-to-event cohort} (\code{one_row_per = "time_to_event"} on
\code{\link[=generate_custom_register]{generate_custom_register()}}), like \code{\link[=scenario_immortal_time]{scenario_immortal_time()}} — but a
genuinely different mechanism, not a variant of the same one. Immortal
time bias is about mishandling a \emph{time-varying exposure}; left
truncation is about mishandling the \emph{time scale} itself under \strong{delayed
entry}: each person has a true age-at-event, but is only observed from
their (later) entry age onward, and is only observed \strong{at all} if they
survived to that age. The baseline hazard here is age-varying (Weibull),
not constant — under a constant hazard the exponential distribution is
memoryless, so there is no bias to demonstrate at all (confirmed during
design, not assumed): ignoring truncation and ignoring it correctly give
the same answer. A fixed baseline covariate (\code{group}, not time-varying —
this scenario has no analogue of \code{ever_exposed}) has a true hazard
ratio. Analyzing \code{Surv(exit_age - entry_age, event) ~ group} ("time
since entry", discarding entry age entirely) instead of correctly
left-truncated \code{Surv(entry_age, exit_age, event) ~ group} (age as the
time scale) biases the recovered hazard ratio. Unlike immortal time,
this bias only appears when \code{true_hazard_ratio} is not \code{1} — with no
true group effect, the naive and correct analyses agree (also confirmed
during design).
}
\details{
The clock here is age, not calendar time, so unlike
\code{\link[=scenario_immortal_time]{scenario_immortal_time()}} this scenario is unaffected by the \code{from}/\code{to}
window passed to \code{\link[=generate_custom_register]{generate_custom_register()}} — there is no equivalent
of \code{horizon_years} to set.
}
