% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate-api.R
\name{generate_custom_register}
\alias{generate_custom_register}
\title{Generate a custom / external register (structure only + optional scenario)}
\usage{
generate_custom_register(
  id,
  one_row_per,
  join_keys = NULL,
  columns = NULL,
  population,
  schema,
  from,
  to,
  seed = NULL,
  scenario = NULL,
  parent = NULL,
  cadence = NULL,
  fidelity = c("clean", "messy"),
  na_rate = NULL,
  outlier_rate = NULL
)
}
\arguments{
\item{id}{Custom register id (not looked up in schema YAML).}

\item{one_row_per}{Grain (see details).}

\item{join_keys}{Character vector of join keys. Defaults to \code{pnr} for
person-side grains; required and household-side for \code{household_year}.}

\item{columns}{CSV path or tibble/data.frame with columns \code{name}, \code{type},
and optional \code{min}, \code{max}, \code{values}. Extra columns (e.g. coefficients)
are ignored — do not put DGP coeffs here. Not used for
\code{one_row_per = "time_to_event"} (see Details).}

\item{population}{Persons table from \code{\link[=generate_background_population]{generate_background_population()}}.}

\item{schema}{Schema from \code{\link[=load_registers_schema]{load_registers_schema()}} (stamps / population).}

\item{from, to}{Window (Date or coercible).}

\item{seed}{Optional RNG seed.}

\item{scenario}{\code{NULL} (independence) or a \code{fiktive_scenario}. Required
(a \code{\link[=scenario_immortal_time]{scenario_immortal_time()}} or \code{\link[=scenario_left_truncation]{scenario_left_truncation()}}) for
\code{one_row_per = "time_to_event"}.}

\item{parent}{Already-generated parent table when
\code{one_row_per = "expand_from_parent"}.}

\item{cadence}{Snapshot cadence for \code{person_reference_date}: \code{"annual"}
(default) or \code{"quarterly"}. Meaningless for \code{one_row_per = "person"}
(one row per person, not a repeated snapshot) -- errors if set there
rather than being silently ignored.}

\item{fidelity}{\code{"clean"} (default) or \code{"messy"}. Not applied for
\code{one_row_per = "time_to_event"} (no safe subset of its fixed columns
for cosmetic noise without corrupting the survival times).}

\item{na_rate, outlier_rate}{Optional fidelity rate overrides in \verb{[0, 1]}.
Same \code{time_to_event} exception as \code{fidelity}.}
}
\value{
A tibble of structural noise that joins on \code{join_keys}.
}
\description{
Front door for researcher-described tables that are \strong{not} in the guide
YAML. Register metadata is passed as R arguments; columns come from a CSV
path or tibble with \code{name}, \code{type}, and optional \code{min} / \code{max} / \code{values}
(structural noise only — \strong{no coefficients}). Pass \code{scenario} to overlay
association / confounding / MNAR / complete-case after the structural draw.
}
\details{
Grains: any existing schema grain (\code{person}, \code{person_reference_date},
\code{event_from_person}, \code{expand_from_parent}, \code{household_year}), plus
\code{time_to_event} (STEP 9: \code{\link[=scenario_immortal_time]{scenario_immortal_time()}} /
\code{\link[=scenario_left_truncation]{scenario_left_truncation()}}). A novel grain is a SCHEMA GAP.
\code{person} and \code{person_reference_date} are \strong{not} aliases, despite the
similar name: \code{person} is exactly one row per eligible person (a
baseline/cohort table -- born after \code{to} excludes a person entirely;
\code{cadence} doesn't apply and errors if set), with \code{referencetid}/\code{year},
when declared as output columns, filled from an independent date drawn
per person within their own \verb{[max(from, foed_dag), to]} window --
faithful to staggered recruitment, but a different meaning from the
snapshot grain's shared as-of date. \code{person_reference_date} is one row
per person \strong{per snapshot date} (annual/quarterly via \code{cadence}) across
the whole window -- a 5-year window is 5 (or 20) rows per person, not 1.
For \code{household_year}, \code{join_keys} must be household-side (e.g.
\code{familie_id}) — never a silent \code{pnr} default. \code{expand_from_parent}
requires an already-generated \code{parent} table. \code{time_to_event} ignores
\code{columns}
entirely — its column shape is fixed (\code{entry_time}/\code{exit_time}/\code{event}/
\code{exposure_start_time}/\code{ever_exposed} for immortal time,
\code{entry_age}/\code{exit_age}/\code{event}/\code{group} for left truncation), because the
correlation between them, driven by \code{scenario}, is the whole point — and
requires a \code{scenario} built with \code{\link[=scenario_immortal_time]{scenario_immortal_time()}} or
\code{\link[=scenario_left_truncation]{scenario_left_truncation()}}; there is no independence version of this
grain.
}
