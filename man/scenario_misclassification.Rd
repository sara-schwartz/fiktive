% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/truth.R
\name{scenario_misclassification}
\alias{scenario_misclassification}
\title{Code misclassification bias scenario (STEP 8d)}
\usage{
scenario_misclassification(
  exposure,
  outcome,
  link = c("identity", "logit", "log"),
  coefficient,
  on = NULL,
  misclass_intercept = -2,
  misclass_coefficient = 0,
  intercept = 0,
  sigma = 1,
  id = "misclassification",
  version = 1L
)
}
\arguments{
\item{exposure, outcome}{\code{register.column} refs.}

\item{link, coefficient}{Association link and effect.}

\item{on}{Column whose values get swapped. Unlike \code{\link[=scenario_mnar]{scenario_mnar()}} /
\code{\link[=scenario_complete_case]{scenario_complete_case()}}, this defaults to \code{exposure}, not \code{outcome}
— code misclassification is usually about the exposure/diagnosis code,
not the outcome.}

\item{misclass_intercept, misclass_coefficient}{Logit intercept / slope
for P(misclassified). The slope multiplies the \strong{outcome's} value
(not \code{on}'s value) — that is what makes misclassification
differential vs. non-differential.}

\item{intercept, sigma}{Association intercept / residual SD.}

\item{id}{Scenario id (default \code{"misclassification"}).}

\item{version}{Integer scenario version.}
}
\value{
A list of class \code{fiktive_scenario}.
}
\description{
Generates an association, then \strong{swaps} a coded column's true value for
a different value already occurring elsewhere in that same column (never
an invented code), with a probability that can depend on the outcome.
Models measurement error in a diagnosis/drug/other code column, e.g. a
mislabeled ICD or ATC code. \code{misclass_coefficient = 0} (the default) is
\strong{non-differential} misclassification: a flat rate from
\code{misclass_intercept} alone, independent of outcome. A non-zero
\code{misclass_coefficient} makes it \strong{differential}: the misclassification
rate itself depends on the outcome's value, which is a materially
different (and often worse) bias than the non-differential case.
}
