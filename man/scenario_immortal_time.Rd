% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/truth.R
\name{scenario_immortal_time}
\alias{scenario_immortal_time}
\title{Immortal time bias scenario (STEP 9)}
\usage{
scenario_immortal_time(
  baseline_hazard = 0.1,
  true_hazard_ratio = 1,
  exposure_rate = 0.2,
  horizon_years = 5,
  id = "immortal_time",
  version = 1L
)
}
\arguments{
\item{baseline_hazard}{Unexposed event hazard per year (positive).}

\item{true_hazard_ratio}{True causal hazard ratio once exposed (positive;
\code{1} = no true effect, useful for demonstrating pure immortal time bias
with nothing else going on).}

\item{exposure_rate}{Hazard per year of \emph{starting} exposure (positive) —
how quickly people who will ever be exposed become exposed.}

\item{horizon_years}{Follow-up length in years, matching the \code{from}/\code{to}
window you intend to pass to \code{\link[=generate_custom_register]{generate_custom_register()}} (e.g.
\code{to - from} in years). \code{expected_naive} is computed from the scenario
alone (\code{make_truth_from_scenario()} never sees the actual generated
data or its window), and the naive bias's exact size genuinely depends
on follow-up length, so this is what lets it be exact rather than a
guess. Defaults to \code{5} (a representative reference window) if omitted
— reasonable for a rough check, but pass your real window length for a
precise one.}

\item{id}{Scenario id (default \code{"immortal_time"}).}

\item{version}{Integer scenario version.}
}
\value{
A list of class \code{fiktive_scenario}.
}
\description{
Generates a \strong{time-to-event cohort} (\code{one_row_per = "time_to_event"} on
\code{\link[=generate_custom_register]{generate_custom_register()}}), not an association overlay on an existing
table like the other scenarios — the whole point is the correlation
between exposure timing and event timing, which can't be expressed as
independent columns plus a later perturbation. Simulates a time-varying
exposure: each person has an independent exposure-start time and an
event/censoring time, where the hazard \strong{increases (or decreases) by
\code{true_hazard_ratio}} from the moment exposure actually starts, not from
cohort entry. Classifying \code{ever_exposed} as a fixed baseline covariate
(\code{coxph(Surv(entry_time, exit_time, event) ~ ever_exposed)}) instead of
correctly time-varying is the classic immortal time mistake: it credits
exposed people with survival time before their exposure even started,
producing spurious protection even when \code{true_hazard_ratio = 1}.
}
\details{
A correctly time-varying analysis (what recovers \code{true_hazard_ratio},
i.e. \code{expected_adjusted}) needs the exposure split into pre/post
intervals, e.g. with \code{survival::tmerge()}:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{cohort$exposure_start_time[is.na(cohort$exposure_start_time)] <- Inf
long <- survival::tmerge(cohort, cohort, id = pnr, event = event(exit_time, event))
long <- survival::tmerge(long, cohort, id = pnr, exposed = tdc(exposure_start_time))
survival::coxph(survival::Surv(tstart, tstop, event) ~ exposed, data = long)
}\if{html}{\out{</div>}}
}
