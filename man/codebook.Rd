% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/codebook.R
\name{codebook}
\alias{codebook}
\title{Column and value labels for a schema register}
\usage{
codebook(schema, register)
}
\arguments{
\item{schema}{Schema from \code{\link[=load_registers_schema]{load_registers_schema()}}.}

\item{register}{One or more schema register ids, e.g. \code{"bef"} or
\code{c("bef", "lmdb")}.}
}
\value{
A tibble, one row per column: \code{name}, \code{label_da}, \code{label_en}
(\code{NA} if the schema doesn't document one), \code{type}, \code{code_system} (\code{NA}
if the column isn't coded), and \code{values} (a single \code{"code: label"}
string per coded value it has a label for, \code{NA} for uncoded columns or
ones with no lookup labels). A \code{register} column is added when
\code{register} has more than one id.
}
\description{
Danish register columns and codes have real names like \code{civst}, \code{hfaudd},
\code{kom} — not self-explanatory — and their coded values (\code{civst = "U"},
\code{koen = 1}) are even less so. registers-guide documents a human-readable
label for almost every column and almost every code, in Danish and
English. \code{codebook()} surfaces that instead of leaving you to read the
schema YAML yourself. It describes the schema, not a specific generated
table — it works the same whether or not you've generated anything yet.
}
