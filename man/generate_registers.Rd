% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate-api.R
\name{generate_registers}
\alias{generate_registers}
\title{Generate several schema registers (opt-in batch)}
\usage{
generate_registers(
  registers,
  population,
  schema,
  from,
  to,
  seed = NULL,
  scenario = NULL,
  fidelity = c("clean", "messy"),
  na_rate = NULL,
  outlier_rate = NULL,
  realistic = FALSE
)
}
\arguments{
\item{registers}{Character vector of schema register ids (required).}

\item{population}{Persons table from \code{\link[=generate_background_population]{generate_background_population()}}.}

\item{schema}{Schema from \code{\link[=load_registers_schema]{load_registers_schema()}}.}

\item{from}{Start of the requested window (Date or coercible).}

\item{to}{End of the requested window (Date or coercible).}

\item{seed}{Optional RNG seed. Restored on exit (per register call).}

\item{scenario}{\code{NULL} (independence) or a \code{fiktive_scenario}.}

\item{fidelity}{\code{"clean"} (default) or \code{"messy"}.}

\item{na_rate, outlier_rate}{Optional fidelity rate overrides in \verb{[0, 1]}.}

\item{realistic}{Default \code{FALSE}. See \code{\link[=generate_register]{generate_register()}} for what
\code{TRUE} opts into (real municipality weighting, sex/age-coherent
diagnosis codes).}
}
\value{
A named list of tibbles, one per requested id (lowercase names).
}
\description{
Builds \strong{only} the named schema registers. \code{registers} is \strong{required} —
there is no silent default of every implemented id. Customs are not accepted
here; use \code{\link[=generate_custom_register]{generate_custom_register()}} one-at-a-time.
}
\details{
When \code{scenario} associations span registers, tables are drawn structurally
first, then the scenario is applied across the list (joining on \code{pnr} when
needed), then fidelity is applied per table.
}
