% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/label-columns.R
\name{label_columns}
\alias{label_columns}
\title{Attach human-readable labels to a generated table's columns}
\usage{
label_columns(data, schema, register, lang = c("en", "da"))
}
\arguments{
\item{data}{A table from \code{\link[=generate_register]{generate_register()}}, \code{\link[=generate_registers]{generate_registers()}},
or \code{\link[=generate_custom_register]{generate_custom_register()}}.}

\item{schema}{Schema from \code{\link[=load_registers_schema]{load_registers_schema()}}.}

\item{register}{The schema register id \code{data} came from, e.g. \code{"bef"}
or \code{"dch"}.}

\item{lang}{\code{"en"} (default) or \code{"da"} -- which label to prefer. Falls
back to the other language for a column that only has one.}
}
\value{
\code{data}, unchanged apart from a \code{"label"} attribute set on each
column the schema documents a label for.
}
\description{
Danish register columns have real names like \code{civst}, \code{hfaudd}, or DCH's
\code{g03011} — the second kind meaningless without a codebook. \code{\link[=codebook]{codebook()}}
already surfaces registers-guide's/fiktive's own label metadata as a
lookup table; \code{label_columns()} attaches that same text as a \code{"label"}
attribute directly on each matching column of an already-generated
table, so it travels with the data (RStudio's Data Viewer and \code{str()}
show it automatically) instead of living in a separate lookup.
}
\details{
Column names are never changed. A column fiktive already understands
well enough to explain isn't necessarily one you should rename it to —
rehearsed analysis code has to run unmodified against the real
register's own names later, so renaming stays something you do
yourself, consistently, on both fake and real data, if you do it at
all. This only adds metadata.
}
