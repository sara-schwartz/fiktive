% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/truth.R
\name{scenario_mnar}
\alias{scenario_mnar}
\title{MNAR missingness bias scenario (STEP 8d)}
\usage{
scenario_mnar(
  exposure,
  outcome,
  link = c("identity", "logit", "log"),
  coefficient,
  on = NULL,
  mnar_intercept = -1,
  mnar_coefficient = 1,
  intercept = 0,
  sigma = 1,
  id = "mnar",
  version = 1L
)
}
\arguments{
\item{exposure, outcome}{\code{register.column} refs.}

\item{link, coefficient}{Association link and effect.}

\item{on}{Column driving / receiving missingness (\code{register.column};
default = outcome).}

\item{mnar_intercept, mnar_coefficient}{Logit intercept / slope for P(missing).}

\item{intercept, sigma}{Association intercept / residual SD.}

\item{id}{Scenario id (default \code{"mnar"}).}

\item{version}{Integer scenario version.}
}
\value{
A list of class \code{fiktive_scenario}.
}
\description{
Generates an association, then sets the target column to NA with probability
depending on its value (informative MNAR). Cosmetic MCAR fidelity stays
orthogonal — use \code{fidelity = "clean"} (default) when evaluating truth.
}
